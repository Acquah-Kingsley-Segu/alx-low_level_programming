#include "main.h"
#include <string.h>

/**
 * _strcat - concatenate a character string to another
 * @dest: character array to be added to
 * @src: character array to add
 * Return: pointer dest
 */

char *_strcat(char *dest, char *src)
{
	/*int len1 = strlen(dest)*/

	/*int len2 = strlen(src);*/

	char *str;

	while (*str != '\0')
	{
		str++;
	}

	str = src;

	return (dest);

}
