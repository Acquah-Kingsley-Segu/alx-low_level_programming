#include "main.h"

/**
 * _strcpy - copies value of one string into another
 * @dest: value to hold copy
 * @src: value to be copied
 * Return: the dest pointer
 */

char *_strcpy(char *dest, char *src)
{
	return (dest = src);
}
